import { useEffect } from "react";

export function useInactivityTimer() {
  useEffect(() => {
    // Stub: wire inactivity timer + return-to-home navigation later.
    return () => {
      // cleanup placeholder
    };
  }, []);
}
